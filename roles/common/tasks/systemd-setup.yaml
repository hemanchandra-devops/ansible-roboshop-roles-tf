- name: copy {{ component }} service
  ansible.builtin.template:
    src: "{{ component }}.service.j2"
    dest: /etc/systemd/system/{{ component }}.service
  notify: demon reload

- name: start, enable {{ component }}
  ansible.builtin.systemd_service:
    name: "{{ component }}"
    state: started
    enabled: yes